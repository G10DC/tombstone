# 🪦 Tombstone

Dead Code & Asset Bloat Hunter for large codebases.

## Overview
Builds entry-point reachability graphs to isolate unreachable symbols, unused exports, orphaned dependencies, and deprecated binary assets.

## Usage
```bash
node lib/tombstone.js --dir <path>
```
